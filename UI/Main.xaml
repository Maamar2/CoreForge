<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="COREFORGE // SYSTEM OPTIMIZER" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#0B0D17" Foreground="#E0E6ED"
        FontFamily="Consolas" FontSize="14"
        SnapsToDevicePixels="True" UseLayoutRounding="True"
        BorderBrush="#00F3FF" BorderThickness="1">

    <Window.Resources>
        <!-- Cyberpunk Palette -->
        <SolidColorBrush x:Key="CyberBlack" Color="#0B0D17"/>
        <SolidColorBrush x:Key="CyberDark" Color="#151922"/>
        <SolidColorBrush x:Key="CyberPanel" Color="#1A1C29"/>
        <SolidColorBrush x:Key="NeonCyan" Color="#00F3FF"/>
        <SolidColorBrush x:Key="NeonPink" Color="#FF00FF"/>
        <SolidColorBrush x:Key="NeonYellow" Color="#FCEE0A"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E0E6ED"/>
        <SolidColorBrush x:Key="TextDim" Color="#8B9BB4"/>

        <!-- Cyber Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CyberPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource NeonCyan}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeonCyan}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="0">
                            <!-- Glitch Effect / Scanline could go here, keeping it simple but sharp -->
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1F2A3D"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource NeonPink}"/>
                                <Setter Property="Foreground" Value="{StaticResource NeonPink}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource NeonCyan}"/>
                                <Setter Property="Foreground" Value="{StaticResource CyberBlack}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl Style (Minimalist Cyber) -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource CyberBlack}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" Background="Transparent" 
                                    BorderBrush="{StaticResource NeonCyan}" 
                                    BorderThickness="0,0,0,0" 
                                    Margin="0,0,5,0">
                                <ContentPresenter x:Name="ContentSite"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                ContentSource="Header"
                                                Margin="10,5"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="Foreground" Value="{StaticResource NeonCyan}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource NeonPink}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GroupBox Style (Neon Borders) -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource NeonCyan}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeonCyan}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5,10,5,5"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- CheckBox Style -->
         <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title Bar simulated area -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="200"/>  <!-- Terminal Output -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CyberPanel}" Padding="15">
            <TextBlock Text="COREFORGE // v1.0" Foreground="{StaticResource NeonCyan}" FontSize="24" FontWeight="Bold" FontFamily="Courier New"/>
        </Border>

        <!-- Main Tabs -->
        <TabControl Grid.Row="1" Margin="10">
            
            <!-- DASHBOARD TAB -->
            <TabItem Header="STATUS_DECK">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="{StaticResource CyberPanel}" BorderBrush="{StaticResource TextDim}" BorderThickness="0,0,0,0" Padding="20">
                        <StackPanel>
                            <TextBlock Text="SYSTEM_DIAGNOSTICS" Foreground="{StaticResource NeonPink}" FontSize="18" Margin="0,0,0,15"/>
                            <TextBlock Text="Waiting for scan..." Name="txtSysInfo" FontFamily="Consolas" Foreground="{StaticResource TextPrimary}"/>
                            <Button Name="btnScanSystem" Content="INIT_SCAN" HorizontalAlignment="Left" Margin="0,20,0,0" Width="150"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{StaticResource CyberPanel}" Margin="20,0,0,0" Padding="20">
                        <StackPanel>
                            <TextBlock Text="SECURITY_GRID" Foreground="{StaticResource NeonPink}" FontSize="18" Margin="0,0,0,15"/>
                            <TextBlock Text="Restore Point: UNKNOWN" Name="txtRestoreStatus" Foreground="{StaticResource TextDim}"/>
                            <TextBlock Text="Updates: PENDING" Name="txtUpdateStatus" Foreground="{StaticResource TextDim}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TWEAKS TAB -->
            <TabItem Header="NEURAL_TWEAKS">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/> 
                        <ColumnDefinition Width="*"/>   
                    </Grid.ColumnDefinitions>
                    
                    <!-- Sidebar -->
                    <Border Grid.Column="0" Background="{StaticResource CyberPanel}" BorderBrush="{StaticResource NeonCyan}" BorderThickness="0,0,1,0">
                        <StackPanel Margin="10">
                            <TextBlock Text="MODULES" Foreground="{StaticResource TextDim}" Margin="5,0,0,10"/>
                            <Button Name="btnModuleAll" Content="ALL_TWEAKS" HorizontalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Margin="0,5"/>
                            <Button Name="btnModulePrivacy" Content="PRIVACY_SHIELD" HorizontalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Margin="0,5"/>
                            <Button Name="btnModulePerformance" Content="PERFORMANCE_CORE" HorizontalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Margin="0,5"/>
                            <Button Name="btnModuleInterface" Content="INTERFACE_MODS" HorizontalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Margin="0,5"/>
                            <Button Name="btnModuleGaming" Content="GAMING_BOOST" HorizontalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Margin="0,5"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Options -->
                    <ScrollViewer Grid.Column="1" Margin="20,0,0,0">
                        <StackPanel>
                            <GroupBox Name="grpPrivacy" Header="TELEMETRY_&amp;_PRIVACY">
                                <StackPanel Margin="10">
                                    <CheckBox Name="chkDisableTelemetry" Content="Disable Windows Telemetry (DiagTrack)"/>
                                    <CheckBox Name="chkDisableLocation" Content="Disable Location Tracking"/>
                                    <CheckBox Name="chkDisableWiFiSense" Content="Disable Wi-Fi Sense"/>
                                </StackPanel>
                            </GroupBox>

                            <GroupBox Name="grpPerformance" Header="PERFORMANCE_OVERDRIVE">
                                <StackPanel Margin="10">
                                    <CheckBox Name="chkHighPerfPlan" Content="Enable Ultimate Performance Plan"/>
                                    <CheckBox Name="chkDisableHibernation" Content="Disable Hibernation (Hiberfil.sys)"/>
                                    <CheckBox Name="chkDisableSysMain" Content="Disable SysMain (Superfetch)"/>
                                </StackPanel>
                            </GroupBox>

                             <GroupBox Name="grpInterface" Header="INTERFACE_&amp;_DVR">
                                <StackPanel Margin="10">
                                    <CheckBox Name="chkGameDVR" Content="Disable Xbox Game DVR"/>
                                    <CheckBox Name="chkExplorerPrefs" Content="Show File Extensions &amp; Hidden Files"/>
                                </StackPanel>
                            </GroupBox>

                            <GroupBox Name="grpGaming" Header="GAMING_BOOST">
                                <StackPanel Margin="10">
                                    <CheckBox Name="chkEnableGameMode" Content="Enable Windows Game Mode"/>
                                    <CheckBox Name="chkOptimizeNetwork" Content="Optimize Network (Disable Nagle's Algorithm)"/>
                                    <CheckBox Name="chkDisablePowerThrottle" Content="Disable CPU Power Throttling"/>
                                    <CheckBox Name="chkGamePriority" Content="Set Game Priority (MMCSS Tweaks)"/>
                                    <CheckBox Name="chkDisableCoreParking" Content="Disable CPU Core Parking"/>
                                    <CheckBox Name="chkHardwareGPU" Content="Enable Hardware-Accelerated GPU Scheduling"/>
                                    <CheckBox Name="chkDisableMouseAccel" Content="Disable Mouse Acceleration"/>
                                </StackPanel>
                            </GroupBox>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Right">
                                <Button Name="btnUndoTweaks" Content="ROLLBACK" Width="120"/>
                                <Button Name="btnApplyTweaks" Content="EXECUTE_TWEAKS" Width="180" Background="{StaticResource NeonCyan}" Foreground="{StaticResource CyberBlack}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- DEBLOAT TAB -->
            <TabItem Header="PURGE_APPS">
                 <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <TextBlock Text="INSTALLED_PACKAGES" Foreground="{StaticResource NeonYellow}" FontSize="16" VerticalAlignment="Center"/>
                        <Button Name="btnScanApps" Content="SCAN_SYSTEM" Width="150" Margin="20,0,0,0"/>
                        <TextBlock Name="txtAppCount" Text="0 apps detected" Foreground="{StaticResource TextDim}" Margin="20,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Border Grid.Row="1" Margin="10" BorderBrush="{StaticResource NeonCyan}" BorderThickness="1">
                        <ListBox Name="lstInstalledApps" Background="{StaticResource CyberPanel}" 
                                 Foreground="{StaticResource TextPrimary}" BorderThickness="0"
                                 SelectionMode="Multiple">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="5">
                                        <TextBlock Text="{Binding Name}" Width="400" FontFamily="Consolas" FontSize="13"/>
                                        <TextBlock Text="{Binding Version}" Width="100" Foreground="{StaticResource TextDim}" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                        <Button Name="btnSelectAll" Content="SELECT_ALL" Width="120"/>
                        <Button Name="btnDeselectAll" Content="DESELECT_ALL" Width="120"/>
                        <Button Name="btnPurgeBloat" Content="PURGE_SELECTED" Width="180" Background="{StaticResource NeonPink}" Foreground="{StaticResource CyberBlack}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- CONFIG TAB -->
            <TabItem Header="CONFIG_NODE">
                <StackPanel Margin="30">
                    <CheckBox Name="chkSafeMode" Content="Always Create System Restore Point (SAFETY_PROTOCOL)" IsChecked="True" Foreground="{StaticResource NeonCyan}"/>
                    <CheckBox Name="chkLogToFile" Content="Save Logs to Local Disk" IsChecked="True"/>
                </StackPanel>
            </TabItem>

        </TabControl>

        <!-- Terminal Output -->
        <Grid Grid.Row="2" Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="{StaticResource NeonCyan}" BorderThickness="0,2,0,0"/>
            
            <TextBlock Text="TERMINAL_OUTPUT_STREAM >_" FontFamily="Consolas" FontWeight="Bold" Margin="10,5,0,0" Foreground="{StaticResource NeonCyan}"/>
            
            <RichTextBox Grid.Row="1" Name="txtTerminal" Background="#050505" Foreground="#00FF00" 
                         FontFamily="Consolas" FontSize="12" Margin="5"
                         VerticalScrollBarVisibility="Auto" IsReadOnly="True" BorderThickness="0">
                <FlowDocument>
                    <Paragraph>
                        <Run Text="CoreForge System Online..."/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Grid>

    </Grid>
</Window>
